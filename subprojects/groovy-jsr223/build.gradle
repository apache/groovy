apply plugin: 'groovy'
apply plugin: 'code-quality'

repositories {
    mavenCentral()
}

dependencies {
    // todo delete this and assume java 6?
    compile "org.livetribe:livetribe-jsr223:2.0.6"
    compile project(':')
    testCompile project(':subprojects:groovy-test')
}
