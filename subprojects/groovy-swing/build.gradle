apply plugin: 'groovy'
apply plugin: 'codenarc'
apply plugin: 'checkstyle'

evaluationDependsOn(':')

dependencies {
    compile project(':')
    groovy project(':')
    testCompile project(':').sourceSets.test.runtimeClasspath
}
