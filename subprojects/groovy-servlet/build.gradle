apply plugin: 'groovy'
apply plugin: 'codenarc'
apply plugin: 'checkstyle'

dependencies {
    compile "javax.servlet:servlet-api:2.4"
    compile "javax.servlet:jsp-api:2.0"
    compile project(':')
    groovy project(':')
    testCompile "jmock:jmock:1.2.0"
    // needed for MarkupBuilder
    compile project(':subprojects:groovy-xml')
    // needed by TemplateServlet
    compile project(':subprojects:groovy-templates')
    testCompile project(':subprojects:groovy-test')
}
