class WhileLoopTest extends GroovyTestCase {

	void testRange() {
	    /** @todo
        x = 0
        y = 5

        while ( y > 0 ) {
            x = x + 1
            y = y - 1
        }

        assert x == 5
        */
    }
}
