language: groovy

jdk:
  - openjdk6
  - oraclejdk7

install: true

script: travis_wait ./gradlew test

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
